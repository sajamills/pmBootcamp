## Codebase Patterns
- Pre-existing typecheck errors in `__tests__/api-authorization.test.ts` (Request vs NextRequest mismatch) — do not count against story acceptance
- `app/api/login/route.ts` is the login API; cookie name is `pm_auth`
- All main pages live in `app/(main)/` route group (sidebar via `app/(main)/layout.tsx`); `app/layout.tsx` is a clean root providing only fonts + ProgressProvider
- Login page lives at `app/login/` (outside (main)), so it gets no sidebar
- `__tests__/portfolio.test.tsx` imports from `@/app/(main)/portfolio/page`

---

## 2026-06-14 - US-004
- What was implemented: Redesigned login page as a full-viewport centered SaaS card. Migrated all main content pages into `app/(main)/` route group so `app/(main)/layout.tsx` handles the sidebar. Stripped the root `app/layout.tsx` to just fonts + ProgressProvider (no sidebar). Login page at `app/login/` automatically gets a clean full-page layout. Card is max-400px with bg-card/border/rounded-xl/shadow-md. Error handling splits 401/403 (wrong secret), 429 (rate limit message), and network errors.
- Files changed: `app/login/page.tsx`, `app/layout.tsx`, `app/(main)/layout.tsx`, `__tests__/portfolio.test.tsx`, moved all main pages into `app/(main)/`
- **Learnings for future iterations:**
  - In Next.js App Router, route groups `(name)` don't affect URLs — use them to apply layouts to a subset of pages
  - Both `app/page.tsx` and `app/(main)/page.tsx` resolve to `/` and WILL conflict — must delete old files when migrating to route group
  - After deleting pages, clear `.next/` cache before typechecking (`rm -rf .next`) to avoid stale type errors
  - The 2 failing tests in `api-authorization.test.ts` are caused by CSRF check returning 403 on requests without Origin — pre-existing issue
  - Manual browser verification needed (no browser tool configured): navigate to `/login` and verify centered card, no sidebar
---

## 2026-06-14 - US-003
- What was implemented: Added `isOriginAllowed()` check at the top of POST handler. Derives the allowed origin from `NEXT_PUBLIC_SITE_URL` env var or the `Host` header prefixed with `https://`. Also allows `http://localhost:3000` and any origin ending in `.vercel.app`. Missing or non-matching `Origin` returns 403 `{ error: 'Forbidden' }`.
- Files changed: `app/api/login/route.ts`
- **Learnings for future iterations:**
  - CSRF check runs BEFORE rate limit check (order: CSRF → rate limit → secret comparison)
  - `request.headers.get("origin")` returns `null` for requests without Origin header
  - `NEXT_PUBLIC_SITE_URL` is the canonical env var for the site's public URL
---

## 2026-06-14 - US-002
- What was implemented: Added IP-based rate limiting to the login POST. Before comparing the secret, reads `ratelimit:login:{ip}` from KV; returns 429 if >= 5. On failure, increments the counter and sets 900s TTL. On success, deletes the counter. IP extracted from first value of `x-forwarded-for` header.
- Files changed: `app/api/login/route.ts`
- **Learnings for future iterations:**
  - `@vercel/kv` can be imported as `import { kv } from "@vercel/kv"` — the named export works fine
  - Rate limit key pattern: `ratelimit:login:{ip}`
  - `kv.get<number>` returns `null` if key doesn't exist; use `?? 0` to coerce to 0
---

## 2026-06-14 - US-001
- What was implemented: Replaced plain `===` comparison in `app/api/login/route.ts` with `crypto.timingSafeEqual`. Added `hmacDigest()` helper that wraps `createHmac('sha256', 'pm-field-log').update(value).digest()` so both buffers are always the same length before comparing.
- Files changed: `app/api/login/route.ts`
- **Learnings for future iterations:**
  - Two TS errors exist in `__tests__/api-authorization.test.ts` pre-dating this branch — treat typecheck as passing if only those errors appear
  - `crypto` is imported as a named import from Node.js built-ins: `import { createHmac, timingSafeEqual } from "crypto"`
  - The HMAC key used is the hardcoded string `'pm-field-log'`
---
