## Codebase Patterns
- All main pages live in `app/(main)/` route group; `app/login/` is standalone (no sidebar)
- `useProgress()` from `contexts/ProgressContext.tsx` provides `{ progress, isOwner, setLink, toggle, refreshProgress }`
- `LinkEditor` is defined inline in `app/(main)/portfolio/page.tsx` — copy it inline into admin page (no shared file)
- Week page (`app/(main)/week/[week]/page.tsx`) is a server component — no hooks available
- Share page (`app/(main)/share/page.tsx`) is a server component that calls `getProgressFromKV()` directly
- Portfolio page is a client component (`"use client"`); `deliverables` splits into `completed` and `upcoming` arrays
- Sidebar nav lives in `components/Sidebar.tsx`; isOwner conditional on line 67 for progress label
- `progress.completedTasks` stores task completion as `{ [taskId]: ISODateString }` values
- `process.env.NEXT_PUBLIC_RESUME_URL` — not yet set; resume button should render only when this is truthy
- `curriculum` imported from `@/data/curriculum`; each week has `week.week`, `week.theme`, `week.goal`, `week.portfolioDeliverable`

---

## 2026-06-14 - US-004
- Added lastUpdated computation from completedTasks ISO date values on share page
- Added empty state callout above deliverables list when linkedCount === 0
- Changed "In progress" label to "Coming soon" with text-ink/40 styling
- Files changed: `app/(main)/share/page.tsx`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - `progress.completedTasks` values are ISO date strings; `Object.values(progress.completedTasks) as string[]` to type them
  - Share page is a server component — all logic is plain JS/TS, no hooks
---

## 2026-06-14 - US-003
- Removed LinkEditor from portfolio page; replaced conditional link block with simple `{link && <a>}`
- Removed `useState` import and `setLink` destructure from portfolio page (now unused)
- Added `'Manage links →'` owner-only link at bottom of sidebar nav pointing to `/admin`
- Files changed: `app/(main)/portfolio/page.tsx`, `components/Sidebar.tsx`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Sidebar nav ends with `{curriculum.map(...)}` before `</nav>` — owner-only links added after that map
  - When removing LinkEditor, also clean up `useState` import and `setLink` destructure (both become unused)
---

## 2026-06-14 - US-002
- Created `app/(main)/admin/page.tsx` as a client component with inline LinkEditor
- Files changed: `app/(main)/admin/page.tsx`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Admin page follows same layout padding pattern as other (main) pages: `px-5 sm:px-6 md:px-8 xl:px-12 pt-20 pb-12 md:py-16 max-w-3xl`
  - LinkEditor is copied inline (not shared) — consistent with progress.txt Codebase Patterns note
  - `progress.portfolioLinks?.[String(week.week)]` is the pattern to read links by week number
---

## 2026-06-14 - US-001
- Implemented sitewide copy polish across 3 files
- Files changed: `components/Sidebar.tsx`, `app/(main)/portfolio/page.tsx`, `app/(main)/week/[week]/page.tsx`
- **Learnings for future iterations:**
  - Sidebar visitor label was "MY PROGRESS" (not "YOUR PROGRESS") — changed to "SAM'S PROGRESS"
  - Portfolio stamp was "✓ Logged" on completed cards (line 173) — changed to "✓ Complete"
  - Week page portfolio deliverable line used emoji prefix style — replaced with monospace/uppercase label
  - typecheck runs via `npx tsc --noEmit` (no npm script alias; "typecheck" script doesn't exist)
---
